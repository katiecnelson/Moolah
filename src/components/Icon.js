import {createIconSetFromIcoMoon} from "react-native-vector-icons";
import icoMoonConfig from "../selection.json";

// Sets up custom icons so that they can be used as components

export default createIconSetFromIcoMoon(icoMoonConfig, "Moolah-Icons");